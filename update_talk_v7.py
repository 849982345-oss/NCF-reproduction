#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""Sync talk script: add PPT page-mapping quick table + new-slide script sections."""
p = r"C:\research-training\repro-ncf\讲稿与QA备忘.md"
t = open(p, encoding="utf-8").read()

quick = """## PPT 页序 ↔ 讲稿速查(临场跟着读)

| 页 | 页面 | 口播提示(PPT 底部浅蓝条同款) |
|---|---|---|
| 1 | 封面 | —(开场自我介绍) |
| 2 | 汇报框架 | 我是 2026 级徐胜钦,跨专业到软件工程,方向推荐系统;今天汇报:一篇论文复现 + 假期学习总结。 |
| 3 | 论文来源与选题理由 | 复现的是 WWW 2017 的 NCF——神经协同过滤的奠基工作,官方开源、有权威对照数字。 |
| 4 | 文献脉络:NCF 的定位 | NCF 承接 Wide&Deep,启发了后面的 SASRec 与 LightGCN——这是我文献调研的脉络。 |
| 5 | 论文核心:三种结构 | 用神经网络替代内积:GMF 线性、MLP 非线性、NeuMF 双路融合,复现中 NeuMF 最好。 |
| 6 | 论文核心公式 | 三种结构的预测式与二分类损失;下面两行是 HR@10 与 NDCG@10 的定义式。 |
| 7 | 复现环境与流程 | 环境 RTX 5070 Ti + PyTorch cu130;数据用论文官方产物,切分与评估协议和论文一致。 |
| 8 | 代码解析(1/2) | 仓库真实代码:三结构 forward 与 1:4 负采样,与论文逐模块核对过。 |
| 9 | 代码解析(2/2) | 训练循环五步;每个 epoch 全量评估 6040 个测试用户,指标 6 位小数落盘。 |
| 10 | 实验流程与实测日志 | 冒烟测试确认链路后,三结构各跑 50 个 epoch,单模型 23 到 35 分钟。 |
| 11 | 复现结果对比表 | NeuMF 复现 0.690 / 0.409,论文 0.688 / 0.410,差 ≤ 0.002;相对趋势完全一致。 |
| 12 | 训练过程曲线 | 三个模型都稳定收敛,约 20 个 epoch 进入平台期,平滑无过拟合。 |
| 13 | 问题分析与踩坑 | 三个坑:数据真伪甄别、新卡 CUDA 兼容、代理下载截断;差距归因分三层。 |
| 14 | 局限性讨论与个人思考 | 如实说三点局限:评估口径、超参与方差、实现差异;下一步想试难负采样。 |
| 15 | 暑期学习总结 | 假期主线是打通最小科研闭环:从读懂论文到跑通实验再到结果对比。 |
| 16 | 具体产物 | 四类产物都可现场查验:代码仓库、实验日志、结果图表、分析文档。 |
| 17 | 谢谢聆听 | 汇报结束,欢迎老师提问;仓库地址在页脚,全部材料可现场查看。 |

> 使用方式:每页底部那条浅蓝色"口播"提示条就是临场可读的一句;完整段落见下方讲稿正文。
> 加 **演示者视图**(WPS:放映 → 演讲者备注 / PowerPoint:幻灯片放映 → 使用演示者视图)可在自己屏幕看到完整讲稿备注。

---

"""

anchor = "# 第一部分 讲稿(可直接照读,按节奏演练)"
assert anchor in t
t = t.replace(anchor, quick + anchor, 1)

# --- P4 literature map section (after 1. 论文来源) ---
anchor2 = "### 2. 论文主要内容(0:35–1:25)"
lit = """### 1.5 文献脉络(P4,约 20 秒)

选它之前我做了文献调研:NCF 承接 2016 年的 Wide & Deep,之后 2018 年 SASRec 把自注意力引入序列推荐,2020 年 LightGCN 把图卷积简化到极致,再往后是对比学习和大模型推荐。NCF 正好在链条的起点,适合作为第一篇复现。检索上我用关键词分层(领域/任务/方法/问题),在 DBLP、Google Scholar、GitHub 多渠道检索,精读用"解决什么/怎么解决/怎么证明"三问框架,文献统一用 Zotero 管理。

"""
assert anchor2 in t
t = t.replace(anchor2, lit + anchor2, 1)

# --- P6 formulas (after 论文主要内容 section, before 代码与实验过程) ---
anchor3 = "### 3. 代码与实验过程(1:25–2:55)"
formula = """### 2.5 核心公式(P6,约 20 秒)

【指公式页】这是论文的核心公式:GMF 的预测式——embedding 逐元素相乘后经输出层;MLP 的前向传播;NeuMF 把两路表示拼接后用 sigmoid 输出;训练用二分类 log loss,每条正样本配 4 条负样本。下面两行是评估指标的定义:HR@10 是命中率,NDCG@10 在命中基础上按排名位置折损,排得越靠前得分越高。

"""
assert anchor3 in t
t = t.replace(anchor3, formula + anchor3, 1)

# --- P8/P9 code slides (inside 代码与实验过程) ---
anchor4 = "过程里有三个值得说的坑:"
code_slides = """【代码页 1(P8)】这是仓库里的真实代码:左边 model.py 的三种结构 forward——GMF 的逐元素相乘、MLP 的拼接进多层网络、NeuMF 的双路拼接融合;右边是负采样——每条正样本采 4 条负样本,并用循环跳过用户已交互的物品。

【代码页 2(P9)】这是训练循环:清梯度、前向、算 loss、反向、更新参数五步;每个 epoch 结束后切到评估模式,对全部 6040 个测试用户做全量评估,然后把指标以 6 位小数写进日志。视频里可以看到我还做了三处工程改造:--model 参数一键切换三结构、逐 epoch 指标落盘、Windows 环境适配。

"""
assert anchor4 in t
t = t.replace(anchor4, code_slides + anchor4, 1)

# --- P14 limitations (after 问题分析 section, before 二、假期学习总结) ---
anchor5 = "## 二、假期学习总结(4:45–7:15)"
lim = """### 5.5 局限性讨论与个人思考(P14,约 30 秒)

我也如实说明这次复现的局限:第一,评估口径是 100 个候选,和全量物品排序不可比,而且论文正文说 100 个负样本、官方数据文件实际是 99 个;第二,我只固定了一组超参,没做网格搜索,这是差距来源之一;第三,单次运行,没有报多随机种子的方差。个人思考:下一步可以先试难负采样,再考虑在物品侧引入属性特征做融合——这两个都是低成本、能跑出对比结果的小改进。

"""
assert anchor5 in t
t = t.replace(anchor5, lim + anchor5, 1)

open(p, "w", encoding="utf-8").write(t)
print("talk script synced, total chars:", len(t))
