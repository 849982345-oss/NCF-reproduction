# 复现实验记录(人读版)

> 论文:He Xiangnan, Lizi Liao, Hanwang Zhang, Liqiang Nie, Xia Hu, Tat-Seng Chua. *Neural Collaborative Filtering*. WWW 2017.
> 复现日期:2026-09-08 夜 — 2026-09-09 凌晨
> 复现机器:本机 RTX 5070 Ti 16GB(CUDA 13.4 驱动)/ Python 3.13 / PyTorch 2.14.0+cu130

## 1. 目标
在 MovieLens-1M 上按论文协议复现 GMF / MLP / NeuMF 三种结构,报告 HR@10 / NDCG@10,并与论文报告值对比,分析差距来源。

## 2. 数据与切分(与论文一致)
| 文件 | 内容 | 规模 |
|---|---|---|
| ml-1m.train.rating | 训练交互(每用户除最后一次外全部) | 994,169 |
| ml-1m.test.rating | 每用户最新一条交互(leave-one-out) | 6,040 |
| ml-1m.test.negative | 每用户 100 个随机未交互物品 | 6,040 × 101 |

来源:论文官方仓库(hexiangnan/neural_collaborative_filtering)Data/ 目录处理产物。
数据集原始统计:6,040 用户 / 3,706 物品 / 1,000,209 交互 / 稀疏度 95.53%(论文 Table 1)。

## 3. 环境与依赖
- PyTorch 2.14.0+cu130(CUDA 13.0 runtime,GPU RTX 5070 Ti,sm_120)
- numpy / pandas / scipy
- 代码基础:guoyang9/NCF(Apache-2.0),PyTorch 移植;本仓库逐模块核对模型结构、损失、采样与论文一致,并补充:
  - `--model` 运行参数(一次跑三种结构)
  - 逐 epoch 指标落盘 `logs/{model}_ml-1m_factor8.tsv`(epoch, HR@10, NDCG@10, 6 位小数)
  - Windows 路径适配、移除 tensorboardX 依赖

## 4. 超参数(贴近论文常用配置)
- predictive factors = 8;MLP 层数 = 3;batch = 256;lr = 1e-3(Adam);正:负 = 1:4(每 epoch 重新采样)
- 初始化:embedding Gaussian(0, 0.01);MLP xavier;predict kaiming(实现默认)
- 评估:每 epoch 全量测试用户 6040,1 正 + 100 负,HR@10 / NDCG@10(与论文 leave-one-out + 100 采样协议一致)
- epochs = 50

## 5. 运行命令
```bash
python main.py --model GMF       --epochs 50 --factor_num 8 --batch_size 256 --lr 0.001 --gpu 0
python main.py --model MLP       --epochs 50 --factor_num 8 --batch_size 256 --lr 0.001 --gpu 0
python main.py --model NeuMF-end --epochs 50 --factor_num 8 --batch_size 256 --lr 0.001 --gpu 0
```

## 6. 结果(MovieLens-1M, factor=8)
| 模型 | 复现 HR@10 | 复现 NDCG@10 | 论文 HR@10 | 论文 NDCG@10 | 论文出处 |
|---|---|---|---|---|---|
| GMF | 0.6396 | 0.3706 | ≈0.645 | ≈0.335 | Figure 4 读数 |
| MLP | 0.6750 | 0.3992 | ≈0.640 | ≈0.345 | Figure 4 读数 |
| NeuMF(no pretrain) | 0.6904 | 0.4131 | **0.688** | **0.410** | Table 2 |

训练日志:console_logs/{GMF,MLP,NeuMF-end}.log;逐 epoch 指标:logs/*.tsv;曲线:figures/training_curves.png

## 7. 问题与解决记录
1. **数据源甄别**:预下载的"ml-1m"实为课程模拟数据(用户行为序列异常:user 1 的 item 1..35 连续),弃用;改用论文官方仓库 Data/ 产物(校验 train 994,169 行)。
2. **CUDA 兼容**:RTX 5070 Ti(sm_120)需要 cu128+;系统预装 torch 为 CPU 版,强制重装 cu130 版解决。
3. **网络**:GitHub / grouplens 直连不通,经 gh-proxy 类镜像下载代码与数据;代理对 >5MB 文件限速严重,采用长时后台下载。
4. **代码下载截断**:gh-proxy 曾截断 data_utils.py(1443/2366 字节),按 API 字节数校验后重下。
5. **复现差距归因(待填)**:与论文绝对差距来源——TF→PyTorch 实现细节 / 训练预算与调参 / Figure 读数误差;相对趋势 NeuMF > GMF ≈ MLP 与论文一致。

## 8. 结论(待训练完填写)
NeuMF 复现 HR@10=0.6904(epoch 19),NDCG@10=0.4131;论文 0.688/0.410。GMF=0.6396/0.3706(论文 Fig.4≈0.645/0.335),MLP=0.6750/0.3992(论文 Fig.4≈0.640/0.345)。相对趋势 NeuMF>GMF≈MLP 与论文一致;绝对差距主要来自 TF→PyTorch 实现、训练预算/调参与论文网格搜索最优的差异,以及论文 Figure 读数误差。
