# 汇报讲稿(8 分钟口述版)+ QA 备忘

> 用法:先通读两遍,重点记【框架句】和 QA。图表/数字以最终运行结果为准,讲稿里 {占位} 处填真实数字。

---

## 开场(约 20 秒)
各位老师好,我是 2026 级硕士研究生徐胜钦。今天汇报两部分:**一篇论文的复现情况**和**暑期学习总结**。

## 一、论文复现(约 4 分 30 秒)

### 1. 选题(约 40 秒)
我复现的是 NCF——Neural Collaborative Filtering,何向南等人 WWW 2017。选它的三个理由:
- 它是神经协同过滤的代表作,是理解"深度学习怎么建模用户-物品交互"的入口;
- 代码规模小,单卡能完整跑通,适合我这种刚入门的研究生做第一次完整复现;
- 论文有公开权威的对照数字(MovieLens-1M 上的 HR@10 / NDCG@10),能检验复现是否到位。

### 2. 论文核心(约 60 秒)
NCF 解决的是协同过滤的核心问题:**怎么建模 user 和 item 的交互**。传统矩阵分解 MF 用内积,表达能力受限。NCF 的答案是:用神经网络学这个交互函数。
它提了三种结构:
- **GMF**:embedding 逐元素相乘(内积的推广),线性交互;
- **MLP**:embedding 拼接后过几层 MLP,学非线性交互;
- **NeuMF**:两者融合,兼顾线性和非线性。
训练用 log loss(二分类:交互过=1),**正负样本 1:4 采样**;评估用 **leave-one-out**:每用户最新一条交互做测试,配 100 个随机负样本,算 HR@10 和 NDCG@10。

### 3. 复现流程(约 60 秒)
环境:RTX 5070 Ti + PyTorch 2.7.1+cu130。数据 MovieLens-1M:6040 用户、3706 物品、100 万交互。我采用论文官方仓库处理好的数据文件,保证切分协议一致。
官方代码是 TensorFlow 1 的,我在社区 PyTorch 移植(guoyang9/NCF)基础上,**逐模块核对模型结构与论文一致**后复现。流程:环境核验 → 数据校验 → 冒烟训练确认链路 → 全量 50 epoch,逐 epoch 记录指标。

### 4. 结果(约 50 秒)
【放对比表】NeuMF 我复现到 HR@10 = {MY_NEUMF_HR},NDCG@10 = {MY_NEUMF_NDCG};论文报告 0.688 / 0.410。GMF、MLP 也都跑通。
关键结论:**相对趋势完全复现**——NeuMF 最好,GMF 和 MLP 接近;与论文的绝对差距在合理范围,主要来自实现与超参差异(下面讲)。

### 5. 问题分析(约 60 秒)
复现中遇到三类问题,也是我收获最大的地方:
1. **数据真伪甄别**:最初用了一份别人预处理好的 ml-1m,结果发现用户行为序列异常(像是模拟数据),弃用后改从论文官方仓库拿处理产物——这让我明白,**实验前必须先核数据来源和协议,否则指标毫无意义**;
2. **环境兼容**:RTX 5070 Ti 是 Blackwell 新架构(sm_120),CPU 版和旧 cu 版 PyTorch 都跑不了 GPU,需要 cu130 版;期间还遇到 GitHub 直连不通,通过代理镜像解决;
3. **复现差距归因**:我的绝对值和论文有差距,归因有三——TF→PyTorch 实现细节、训练轮数与调参策略、论文图读数的近似误差。**相对趋势一致说明复现是可信的**。

## 二、暑期学习总结(约 3 分钟)
暑期主线是**为科研打基础 + 完成一次独立复现**:
- **学习内容**:科研方法论(文献检索与精读——三级阅读法、读论文三问:解决什么问题/怎么解决/怎么证明)、PyTorch 实验链路(数据加载、模型、训练、评估)、Python/数据分析基础、Git 工作流;
- **完成工作与产物**:系统性入门推荐系统(协同过滤、序列推荐、隐式反馈、评估协议);这次 NCF 复现的完整代码仓库,包含 README、实验日志、逐 epoch 指标和图表;
- **遇到的问题**:新卡 CUDA 兼容、数据源甄别、论文数字与复现差异怎么归因(前面已讲);
- **后续计划**:①跟组会和导师方向对齐,我了解到组里方向涉及开源生态/多智能体/负责任推荐,准备系统补图神经网络和 LLM 推荐方法;②从"复现"走向"改进",打算先拿 NCF 的负采样策略练手,做一个小改进对比。

我的汇报到这里,谢谢各位老师,请批评指正。

---

# QA 备忘(3 分钟提问环节)

## 高频问题与答法

**Q:为什么复现这篇,不选更新的模型?**
A:复现的价值在方法论的完整性——NCF 代码规模适合第一次做端到端复现;它和 SASRec、LightGCN 等后续工作一脉相承,打好这个底子再看 GNN/Transformer 推荐会顺很多。而且我要验证的不是"新",是"我能不能把一篇论文从读到跑通到对比做完整"。

**Q:GMF 和 MF(矩阵分解)是什么关系?**
A:GMF 是 MF 的神经网络推广。MF 用内积打分,GMF 把 embedding 逐元素相乘后再过一个线性层,等价于学了每个维度的权重,比固定内积更灵活。

**Q:为什么 NeuMF 比单独 GMF/MLP 好?**
A:GMF 擅长表达线性交互,MLP 擅长非线性,两者互补;NeuMF 融合两路,论文实验和我的复现都显示它最好。代价是参数更多、训练更慢。

**Q:HR@10 和 NDCG@10 区别?**
A:HR 只看真实目标在不在前 10(命中率);NDCG 还看命中的位置,排越靠前分越高。一个模型可能 HR 高但 NDCG 低——说明命中但排得靠后。

**Q:负采样 1:4 怎么来的?换成别的比例会怎样?**
A:论文实验部分测试了不同负采样率(论文 Figure 7),1:4 是效果和效率的折中。负样本太少学不到"不喜欢"的信号,太多训练变慢且可能过拟合高频负样本。

**Q:你的数字和论文差多少?差在哪?**
A:(用真实差值答)差 X 个点。归因:①官方是 TF1 实现,我用的 PyTorch 移植有实现细节差异;②训练预算和调参不完全一致,论文在多个 batch/lr 里选最优,我只固定了一组;③论文 Figure 的数字是图读的,有误差。相对趋势(NeuMF>GMF≈MLP)完全一致,说明复现可信。

**Q:为什么用 leave-one-out,不用随机切分?**
A:推荐场景是时序的,随机切分可能把未来交互放进训练集,造成信息泄漏,指标虚高。leave-one-out 用每用户最新交互做测试,更贴近真实。

**Q:数据集统计?**
A:MovieLens-1M:6,040 用户、3,706 部电影、1,000,209 条评分,稀疏度 95.53%(论文 Table 1)。

**Q:训练耗时/显存?**
A:(填真实值)单模型 50 epoch 约 X 分钟,显存峰值约 X GB(模型很小,主要开销在评估全量用户)。

**Q:下一步打算做什么?**
A:短期补 GNN/LLM 推荐的基础;中期想在 NCF 这类框架上做一个小的改进实验(比如负采样策略、或融合物品属性),跑出对比结果再跟老师讨论。

## 被问到不会时的保命句
- "这块我还没深入,我理解的是……(说半句)……会后我查证一下再向您汇报。"(别编!)
- "论文实验部分我核对了协议,但某个实现细节我需要回去翻代码确认。"

## 汇报现场清单
- [ ] PPT 拷贝到汇报电脑(或用自己电脑,备 HDMI)
- [ ] 结果数字已填、图表已插入
- [ ] 代码仓库能现场打开(logs/ 目录可展示)
- [ ] 8 分钟计时演练至少 2 遍(超时是最大的减分项)
