# 开学汇报 · 发言稿 + 材料展示指南 + QA 备忘

> 汇报人:徐胜钦 · 8 分钟口述 + 3 分钟提问
> 讲稿按通知要求两部分组织:①论文复现(来源/主要内容/代码与实验过程/复现结果及问题分析)②假期学习总结(学习内容/完成的工作/具体产物/遇到的问题/后续计划)
> 全文口播约 7 分 30 秒,预留 30 秒缓冲。数字均为真实跑出的结果,可现场查验。

---

# 第一部分 讲稿(可直接照读,按节奏演练)

## 开场(0:00–0:15)

各位老师好,我是 2026 级硕士研究生徐胜钦,本科是新能源科学与工程,跨专业到软件工程,研究方向是推荐系统。下面我汇报两部分:一篇论文的复现情况,和假期学习总结。

## 一、论文复现(0:15–4:45)

### 1. 论文来源(0:15–0:35)

我复现的是 **Neural Collaborative Filtering**,中文叫神经协同过滤,何向南等作者,**发表于 WWW 2017**。这篇论文是我在文献调研阶段,从文献库的推荐系统方向基础文献中选定的:它是"用深度学习做协同过滤"这条线的奠基工作,被引数千次;官方代码在 GitHub 上公开,是 TensorFlow 1 实现;论文报告了 MovieLens-1M 上的 HR@10 和 NDCG@10 指标,有明确的可对照数字。

### 2. 论文主要内容(0:35–1:25)

论文要解决的核心问题是:**协同过滤里怎么建模用户和物品之间的交互**。传统矩阵分解用内积来表达交互,表达能力受限。论文的思路是用神经网络来学习这个交互函数,提出了三种结构:

- **GMF**:把用户和物品的 embedding 逐元素相乘,可以看成内积的神经网络推广,建模线性交互;
- **MLP**:把两个 embedding 拼接后送进多层感知机,建模非线性交互;
- **NeuMF**:把 GMF 和 MLP 两路融合后打分,论文实验里效果最好。

训练上,论文用隐式反馈做二分类,log loss,每条正样本采 4 条负样本;评估上,用 leave-one-out——每个用户最后一次交互作为测试目标,再随机配 99 个未交互物品,在这 100 个候选里算 HR@10 和 NDCG@10。

### 3. 代码与实验过程(1:25–2:55)

我的复现环境是**本机 RTX 5070 Ti 16G 显卡,PyTorch 2.14 加 cu130**。

数据方面,我用的是**论文官方仓库处理好的 MovieLens-1M 数据**:训练集 99.4 万条交互,测试集是 6040 个用户、每个用户 1 个真实目标加 99 个随机负样本——这个切分协议和论文完全一致。

代码方面,论文官方是 TensorFlow 1 的实现在新机器上很难跑,我以社区验证过的 PyTorch 移植实现为基础,**逐模块核对了模型结构、损失函数、负采样和评估逻辑与论文一致**,并做了三处工程改造:加了模型选择参数、把每个 epoch 的指标以 6 位小数落盘成日志文件、适配了 Windows 环境。

实验流程是:环境核验 → 数据校验 → 先用 2 个 epoch 冒烟测试确认整条链路 → 再对 **GMF、MLP、NeuMF 三种结构各跑 50 个 epoch**,每个 epoch 都在 6040 个测试用户上做全量评估。三个模型在 5070 Ti 上各跑了 23 到 35 分钟。

过程里有三个值得说的坑:

第一,**数据真伪**:我最初拿到一份别人预处理的数据,第一眼看着能跑,但抽查发现用户行为序列异常,判断是模拟数据,直接弃用,改从论文官方仓库取数据——这让我意识到,实验前必须先核数据来源和协议,否则跑出来的指标没有意义。

第二,**显卡兼容**:RTX 5070 Ti 是新的 Blackwell 架构,需要 cu128 以上的 PyTorch;机器上预装的是 CPU 版,我卸载后强制重装了 cu130 版本,CUDA 才可用。

第三,**网络问题**:复现期间 GitHub 和数据集官网直连不通,是用代理镜像拿到的代码和数据;代理还会截断大文件,我按官方文件的字节数逐个校验后重新下载,才保证了数据完整。

### 4. 复现结果(2:55–3:45)

【切结果页】这是三种结构的复现结果,对照论文报告值:

- **NeuMF 是我复现到的最好结果:HR@10 是 0.690,NDCG@10 是 0.409;论文 Table 2 报告的 no-pretraining 结果是 0.688 和 0.410——两项差距都在 0.002 以内,可以认为复现成立**;
- GMF 复现 HR@10 是 0.640,论文图里读数约 0.645;MLP 是 0.675;
- 关键的相对趋势 **NeuMF 最好、GMF 和 MLP 接近,和论文结论完全一致**。

【切曲线页】这是三个模型 50 个 epoch 的训练曲线:都在 20 个 epoch 左右进入平台期,曲线平滑,没有明显的过拟合。

### 5. 问题分析(3:45–4:45)

复现结果和论文还有零点几到三点几个百分点的差异,我做了归因,主要有三方面:

第一,**实现差异**。论文官方是 TensorFlow 1 加 Keras 实现,我用的是 PyTorch 移植,在初始化、MLP 层结构这些细节上会有不同,比如 MLP 的网络宽度,导致 GMF 和 MLP 的指标比论文图里的读数要高一些。

第二,**训练预算和调参策略不同**。论文在 batch size 和学习率的多组组合里做网格搜索取最好的结果,我只固定了一组常用的配置,没有做超参数搜索。

第三,**对照数据的口径**:GMF 和 MLP 的论文数字我是从 Figure 4 里读出来的,有正负 0.005 左右的误差;NeuMF 对照的是 Table 2 的精确值,所以 NeuMF 这组对比最严格、也最可信。

这里我想说一句体会:**相对趋势完全一致,说明复现是可信的;绝对值有差异,也正常——复现的价值在于搞清楚差异从哪来,而不是把数字凑到一样**。

## 二、假期学习总结(4:45–7:15)

### 1. 学习内容(4:45–5:10)

假期我的学习围绕"能不能独立完成一次论文复现"这条主线展开,分四块:一是**科研方法论**——文献检索、论文精读的方法,比如读一篇论文先回答三个问题:它解决什么问题、怎么解决、怎么证明;二是**PyTorch 的实验全链路**——数据加载、模型、训练、评估这一整套;三是**Python 和数据分析基础**;四是 **Git 工作流**,把代码和实验记录管起来。

### 2. 完成的工作(5:10–5:35)

具体完成了两件事:一是系统梳理了推荐系统的基础知识框架,包括协同过滤、隐式反馈、序列推荐和评估协议;二是**独立完成了 NCF 这篇论文从读到跑通到对比的完整复现**,包括三种结构的实验和结果分析。

### 3. 具体产物(5:35–6:00)

产物一共有四类,都可以现场查验:
**第一是代码仓库**,已经推到 GitHub 上公开,包含复现代码、论文官方数据和运行脚本;
**第二是实验日志**,每个模型每个 epoch 的 HR@10、NDCG@10 都记在案,6 位小数,训练时终端输出也完整保留;
**第三是结果图表**,就是刚才看到的对比表和训练曲线;
**第四是这份汇报 PPT 和分析文档**。

### 4. 遇到的问题(6:00–6:30)

问题前面复现部分讲了三个技术层面的;学习层面最大的问题是**跨专业**——我本科是新能源,计算机基础和科班同学有差距,所以假期选择"先跑通一个最小闭环"的策略:不贪多,先把一篇论文完整做下来,建立"能看懂、能运行、能解释"的底气。

### 5. 后续计划(6:30–7:15)

分三步:
**近期**,跟组会和导师的方向对齐,补图神经网络和基于大模型的推荐方法这两块基础知识;
**中期**,从复现走向改进——我打算先拿这次复现的框架做一个小实验,比如改负采样策略或者加入物品属性信息,跑出对比结果再和老师讨论;
**长期**,希望在导师指导下找到一个具体的研究问题,做出自己的第一篇工作。

## 收尾(7:15–7:30)

我的汇报到这里,代码仓库、实验日志和结果图表都可以现场查看,欢迎各位老师批评指正。

---

# 第二部分 材料展示指南(怎么展示代码仓库 / 实验日志 / 结果图表)

## 核心原则
**PPT 讲主线,材料做证据。** 8 分钟主体用 PPT;导师追问或要求"打开看看"时,按下面的动线展示。所有材料都有**本地版本**,不怕现场网络不稳。

## 0. 现场准备(汇报前一晚做)

- [ ] 本地目录确认可用:`C:\research-training\repro-ncf\`(仓库全量)和 `C:\research-training\开学汇报_徐胜钦_交付\`(交付包)
- [ ] PPT 拷 U 盘 + 邮箱/云盘各一份;PDF 版也带上(防字体问题)
- [ ] 浏览器提前登录 GitHub,收藏仓库地址:**https://github.com/849982345-oss/NCF-reproduction**(网络不好时直接展示本地 VS Code)
- [ ] VS Code 提前打开 `C:\research-training\repro-ncf` 文件夹(收藏 main.py、logs 目录)
- [ ] 演练一次 8 分钟计时

## 1. 代码仓库怎么展示(推荐动线 4 步,约 2 分钟)

**展示位置:浏览器 GitHub 页面,或 VS Code 打开本地仓库(推荐,不怕网)**

**第 1 步 · README(30 秒)** — 打开仓库首页
> 话术:"README 里写了论文来源、环境配置、复现命令和最终结果表,任何人拿到这个仓库都能复现。"

**第 2 步 · 训练核心代码(40 秒)** — 打开 `ncf_pytorch/main.py`,滚到训练循环
> 话术:"这是训练循环,五个步骤:清梯度、前向、算 loss、反向、更新参数;每个 epoch 结束后在 6040 个测试用户上做全量评估,并把指标写进日志文件。"
> 指这几行:`model.zero_grad()` → `prediction = model(user, item)` → `loss.backward()` → `optimizer.step()`

**第 3 步 · 模型结构(30 秒,可选)** — 打开 `ncf_pytorch/model.py`
> 话术:"模型类同时支持 GMF、MLP、NeuMF 三种结构,和论文一一对应:这里是 embedding,这里是逐元素相乘的 GMF 分支,这里是拼接后过 MLP 的分支。"

**第 4 步 · 数据协议(20 秒,可选)** — `data_utils.py`
> 话术:"数据加载和负采样逻辑——训练时每条正样本采 4 条负样本,并且跳过用户已经交互过的物品。"

## 2. 实验日志怎么展示(约 1 分钟)

**文件位置**:`ncf_pytorch\logs\NeuMF-end_ml-1m_factor8.tsv`(逐 epoch 指标,tab 分隔:epoch / HR@10 / NDCG@10,6 位小数)
另有 `console_logs\NeuMF-end.log`(完整终端输出,含每个 epoch 耗时)

**展示方式 A(推荐,最直观)**:VS Code 打开 tsv 文件,展示首尾
> 话术:"每一行是一个 epoch 的结果:第 0 轮 HR 只有 0.59,第 19 轮涨到 0.6904——也就是最优结果,后面稳定在 0.68 左右。这是 6 位小数的原始记录,不是四舍五入后的。"

**展示方式 B(开终端,有说服力)**:在仓库目录执行两条命令
```bash
tail -n 5 ncf_pytorch/logs/NeuMF-end_ml-1m_factor8.tsv     # 看最后 5 个 epoch 的指标
grep "End. Best" console_logs/*.log                        # 直接看三个模型的最优结果
```
> 话术:"日志每条都是训练时自动写盘的,时间戳和 epoch 耗时都能对上,说明不是为了汇报后补的。"

**如果导师问"训练曲线有没有波动/过拟合"**:指向曲线页——"曲线平滑,验证指标没有回落,没有过拟合迹象。"

## 3. 结果图表怎么展示(约 30 秒)

**三处都有,按场合选:**
1. **PPT 第 6 页**(结果对比表):汇报主用——讲话时停在表格上,念关键数字(NeuMF 0.690/0.409 vs 论文 0.688/0.410);
2. **PPT 第 7 页**(训练曲线图):讲"训练过程"时用;
3. **本地原图**:`figures\training_curves.png`(200dpi 高清),或交付包里的 `复现训练曲线.png` —— 导师要"看清曲线细节"时直接打开图片;
4. **交付包预览**:`开学汇报_徐胜钦_交付\预览.html`,浏览器打开可翻 10 页高清渲染图。

## 4. 三种场景的兜底方案

| 场景 | 做法 |
|---|---|
| 正常汇报 | PPT 讲完,材料只做"可现场查验"的口头说明,不主动展开 |
| 导师要求看代码/日志 | 按上面动线 1→2→3,总时长控制在 3 分钟内;只展示 README→训练循环→日志→曲线这 4 处,不要漫游文件树 |
| 无网络/外部机器 | 用 U 盘里的交付包:PPT + PDF + `render/` 渲染图 + 备用截图;口头说明"代码仓库在 GitHub 上,本地也有一份完整副本" |

## 5. 素材备份清单(拷 U 盘)

- [ ] 开学汇报_徐胜钦.pptx + .pdf
- [ ] 交付说明.md(里面有 GitHub 链接,方便导师扫)
- [ ] 复现训练曲线.png(原图)
- [ ] render/ 10 张渲染图(PPT 打不开时的兜底)
- [ ] (可选)代码仓库 zip 压缩包

---

# 第三部分 QA 备忘(3 分钟提问环节)

## 关于论文与复现
**Q:为什么选这篇复现,不选更新的模型?**
A:复现的价值在方法论的完整性。NCF 是神经协同过滤的奠基工作,代码规模适合第一次做端到端复现;它和后续的序列推荐、图神经网络推荐一脉相承,打好这个底子再看新方法会更顺。我想验证的不是"新",是"我能不能把一篇论文从读到跑通到对比做完整"。

**Q:GMF 和传统矩阵分解是什么关系?**
A:GMF 是 MF 的神经网络推广。MF 用内积打分,GMF 把 embedding 逐元素相乘后过一个线性层,相当于让模型自己学每个维度的权重,比固定内积灵活。

**Q:为什么 NeuMF 比 GMF、MLP 单独用更好?**
A:GMF 擅长线性交互,MLP 擅长非线性,两者互补;NeuMF 融合两路,论文实验和我的复现都显示它最好,代价是参数更多、训练更慢。

**Q:HR@10 和 NDCG@10 什么区别?怎么算的?**(高频必答)
A:两个指标都是"每个用户 100 个候选(1 个真实的下一部电影 + 99 个随机负样本),看真实那个排第几"。
- **HR@10 是命中率**:真实物品进前 10 记 1,没进记 0,6040 个用户取平均。我的结果 0.6904,意思是**69% 的用户真实物品进了前 10**。
- **NDCG@10 还看位置**:排第 1 名记满分 1,第 2 名是 1/log₂3≈0.63,第 3 名 0.5,第 7 名 0.33,没进前 10 记 0,再取平均。我的结果 0.4086。
- 一句话:**HR 管进没进圈,NDCG 管进了圈站多靠前**,所以 NDCG 永远小于等于 HR。
- 举例(我模型实测):user#0 真实物品排第 2 → HR=1、NDCG=0.63;user#2 排第 7 → HR=1、NDCG=0.33;user#10 排第 28 → 两个都是 0。全体里排第 1 的 1100 人、进前 10 的 4170 人。
- 提醒:**这类指标只在相同数据、相同切分、相同候选集口径下才能对比**;换成全量物品排序 HR 会低得多。

**Q:为什么用 leave-one-out,不随机切分?**
A:推荐是时序场景,随机切分会把未来的交互放进训练集,造成信息泄漏、指标虚高。leave-one-out 用每个用户最新一次交互做测试,更接近真实场景,也是论文的协议。

**Q:负采样 1:4 是怎么定的?**
A:论文实验里测试过不同的负采样比例(Figure 7),1:4 是效果和开销的折中。负样本太少模型学不到"不喜欢"的信号,太多会拖慢训练、也可能过拟合热门负样本。

**Q:你的数字和论文差多少?差在哪?**(核心问题,照实说)
A:NeuMF 的 HR@10 是 0.690 对论文 0.688,NDCG@10 是 0.409 对 0.410,差都在 0.002 以内。差异来源有三:一是论文官方是 TensorFlow 1 实现,我用的 PyTorch 移植在初始化、MLP 层宽这些细节上有差异,这也是 GMF、MLP 指标略高于论文图读数的原因;二是论文做了超参数网格搜索取最优,我只固定了一组常用配置;三是 GMF、MLP 的论文数字是我从 Figure 4 读的,有 ±0.005 的读数误差,NeuMF 对照的是 Table 2 精确值,所以这组最严格。相对趋势 NeuMF > GMF ≈ MLP 与论文完全一致,复现结论成立。

**Q:数据集统计?**
A:MovieLens-1M:6040 用户、3706 部电影、约 100 万条交互,稀疏度 95.53%;训练用 99.4 万条,测试 6040 个用户、每个用户 100 个候选。

**Q:训练耗时、显存?**
A:三个模型各 50 个 epoch,单模型 23 到 35 分钟(5070 Ti);模型很小,参数量百万级以内,显存占用远低于 16G,主要时间花在每个 epoch 对全部测试用户做评估。

**Q:如果让你改进这篇论文的方法,你想改什么?**
A:(这是给你展示"后续计划"的机会)我想到两个低成本方向:一是负采样策略,现在是均匀采样,可以试 popularity-based 难负样本;二是引入物品属性做多模态融合。我准备先做第一个的小实验,跑出对比结果再向老师汇报。

## 关于学习总结
**Q:假期最大的收获是什么?**
A:建立了一个完整的科研闭环:读论文 → 核对代码 → 跑通实验 → 分析差异。以前我看论文只停留在"看懂",现在我知道一个指标背后有哪些协议细节会影响结果。

**Q:你本科不是计算机,怎么补基础?**
A:策略是不贪多、抓主线:先用最小闭环建立信心,再回头补数学和代码基础。目前 Python、PyTorch 的常用部分已经能独立使用,算法和数学基础还需要持续补。

**Q:接下来打算做什么?**
A:近期跟组会对齐方向,补图神经网络和大模型推荐的基础;中期做负采样策略的小改进实验;长期希望在导师指导下确定具体研究问题,产出自己的第一篇工作。

## 被问到不会时的保命句
- "这个细节我还没有深入,我的理解是……(说半句),会后我查证了再向您汇报。"(**千万别编**)
- "论文的实现细节我需要回去再翻一遍代码确认,目前只能说到协议这一层。"

## 演示现场禁忌
- 不要在汇报中漫游文件树、现场搜索文件——所有要展示的东西提前打开好标签页/文件
- 不要现场打字运行训练(时间不允许);要展示"能跑"就展示日志和已有输出
- 8 分钟超时是最大的减分项:每个部分练到能在标记时间内讲完,宁可少讲一句
